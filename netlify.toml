[build]
  command   = "npm run build"
  publish   = "dist"
  functions = ".netlify/functions-internal"  # where Nitro actually emitted your lambda

[[redirects]]
  from   = "/*"
  to     = "/.netlify/functions/server"
  status = 200
  force  = true
