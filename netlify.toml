[build]
  command   = "npm run build"
  publish   = "dist"
  functions = ".netlify/functions"

# All routes go to your SSR function
[[redirects]]
  from     = "/*"
  to       = "/.netlify/functions/server"
  status   = 200
  force    = true
  
[functions]
  directory = ".netlify/functions"